{
  "name": "open-tekken",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "ready": "vp check && vp test && vp run test:tools && vp run -r build",
    "dev": "vp run website#dev",
    "test:tools": "node --test tools/t5-rom/*.test.mjs",
    "prepare": "vp config"
  },
  "devDependencies": {
    "vite-plus": "catalog:"
  },
  "engines": {
    "node": ">=22.12.0"
  },
  "packageManager": "pnpm@11.9.0"
}
